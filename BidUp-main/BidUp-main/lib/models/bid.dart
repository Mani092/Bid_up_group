import 'package:flutter/material.dart';

class liveBidding extends StatelessWidget {
  const liveBidding({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
